# d — Binance Red Packet araç seti

- **`kod_radar.py`** (1. nesil): çok kaynaklı kod tarayıcı — tek betik, `python3 kod_radar.py --once`
- **`redpacket_bot/`** (2. nesil 🚀): tam otonom, modüler, öğrenen sistem —
  [README](redpacket_bot/README.md)

```bash
cd redpacket_bot
python3 main.py --once     # güvenli simülasyon testi
python3 main.py            # otonom döngü (simülasyon)
```
